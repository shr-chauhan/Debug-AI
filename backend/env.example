# Database configuration
# Option 1: Use full DATABASE_URL (alternative to individual components below)
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/error_ingestion

# Option 2: Use individual components (recommended)
DATABASE_USER=postgres
DATABASE_PASSWORD=postgres
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=error_ingestion

# Environment configuration
ENV=development  # development, production, etc.

# Optional: Server configuration
HOST=0.0.0.0
PORT=8000

# Redis configuration (for Celery)
REDIS_URL=redis://localhost:6379/0

# OpenAI configuration (for AI error analysis)
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o-mini  # or gpt-4, gpt-3.5-turbo, etc.

# GitHub configuration (optional, for fetching source code)
# Default token (used as fallback if project-specific token not found)
# GITHUB_TOKEN=your_github_personal_access_token_here

# Project-specific tokens (optional)
# Pattern: {PROJECT_KEY}_TOKEN (project key in uppercase, hyphens replaced with underscores)
# Examples:
# DEBUG_AI_TOKEN=ghp_token_for_debug_ai_project
# MY_APP_TOKEN=ghp_token_for_my_app_project
# PRODUCTION_API_TOKEN=ghp_token_for_production_api
#
# Token lookup order:
# 1. Token explicitly set in repo_config.access_token (if provided)
# 2. {PROJECT_KEY}_TOKEN environment variable (project-specific)
# 3. GITHUB_TOKEN environment variable (fallback)

